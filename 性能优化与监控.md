# Kaikaio 性能优化与监控计划

> 目标：以「优化 App 性能 + 持续监控 App 性能」为主线，建立可量化的性能指标体系，依托已接入的 Sentry 搭建线上性能监控，并分阶段完成启动、列表、内存、包体积等维度的优化。
>
> 技术基础：React Native 0.81.5（Hermes）、Expo SDK 54、`@sentry/react-native@~7.2.0`（已接入崩溃监控 + 导航面包屑 + Feedback）、zustand、AsyncStorage、reanimated 4。
>
> 关联文档：`LIST_TRANSITION_OPTIMIZATION.md`（列表页动效与 memo 化已全部完成，本计划不再重复，仅在其基础上做数据化验证）。
>
> 状态标记：`[ ]` 未开始 / `[x]` 已完成

---

## 一、现状盘点

| 维度 | 现状 | 结论 |
| --- | --- | --- |
| 崩溃监控 | Sentry（`utils/sentry.ts` initSentry）+ 本地 crashLogger 双层兜底；ErrorBoundary 同步上报；导航面包屑已注册 | ✅ 已具备，无需重建 |
| 性能监控 | 仅 `tracesSampleRate: 0.2`，未开启帧率采集、未定义关键事务、无自定义 span、无告警规则 | ❌ 空白，本计划核心补齐项 |
| 列表性能 | `pages/List.tsx` 已完成 memo 化 + FlatList 调优 + reanimated 动画迁移（见 LIST_TRANSITION_OPTIMIZATION.md） | ✅ 有基础，缺线上帧率数据验证 |
| 启动性能 | `App.tsx` 一次性挂载全部 Provider 与页面注册；AuthLoading 用 AsyncStorage 读 token 后 replace；无首屏耗时度量 | ⚠️ 有优化空间，先度量后优化 |
| 数据层 | AsyncStorage 分散读写（billStore、configSync 等）；无批量/预热策略 | ⚠️ 启动路径上的串行读取是潜在瓶颈 |
| 包体积 | 依赖含 gifted-charts、svg、reanimated carousel、node-forge 等；未做过依赖审计 | ⚠️ 待审计 |

---

## 二、性能指标体系（先度量，后优化）

| 指标 | 定义 | 采集方式 | 目标阈值（P75） |
| --- | --- | --- | --- |
| 冷启动时长 | 进程启动 → 首屏（List 页）可交互 | Sentry App Start span（TTID/TTFD）+ 自定义首屏完成 span | < 1.5s（中端机） |
| 页面切换耗时 | 导航切换 → 新页面 TTFD | Sentry reactNavigationIntegration 自动事务 | < 300ms |
| 慢帧/冻帧率 | Slow frames / Frozen frames 占比 | Sentry Frames 采集（随事务上报） | 慢帧 < 5%，冻帧 < 0.5% |
| 记账提交耗时 | 点击提交 → 本地落库完成（乐观更新前） | 自定义 span | < 100ms |
| 账单列表加载耗时 | fetchBills 开始 → setData 完成 | 自定义 span（含缓存命中分支） | 缓存 < 50ms，网络 < 1s |
| JS FPS / UI FPS | 开发期手动巡检 | DevTools / Flashlight / 真机性能监视器 | 滚动 ≥ 55 FPS |
| 内存峰值 | 账单量大月份 + 统计图表页 | Xcode Instruments / Android Profiler | 无持续增长（无泄漏），峰值 < 300MB |
| 包体积 | Android APK / iOS IPA 安装包大小 | 每次 release 记录基线 | 逐版本不异常增长（±2% 内需说明） |

**原则**：所有优化动作必须「先有基线数据 → 优化 → 对比数据」，禁止凭感觉优化。

---

## 三、监控体系建设（P0）

### P0-1 Sentry Performance 能力补全

**状态**：[x] 已完成（实施备注：`utils/sentry.ts` initSentry 已补充 `tracesSampleRate: 0.5`（基线积累期，稳定后回落 0.1~0.2）、`profilesSampleRate: 0.2`（相对 traces，整体 10% 事务带 profile），并显式声明 `enableAppStartTracking` / `enableNativeFramesTracking` / `enableStallTracking`（SDK 7.2 中默认均为 true，显式固化防误关）；选项名已按 node_modules 中 SDK 类型定义核实，非文档旧称 `enableAppStartTracing`。release 对齐：未在 init 中手动覆盖 `release`/`dist`，沿用 SDK 原生默认命名（`bundleId@versionName+versionCode`，版本号由 scripts/sync-version.js 从 package.json 同步），与 android/app/build.gradle 引入的 sentry.gradle 上传命名一致，符号化不会错位；`android/sentry.properties` org/project 与 app.json plugin 一致（kaikaio-lg / react-native），已核对。后续验证项：发版后在 Sentry Performance 面板确认 App Start 事务、导航事务带 frames measurements，慢事务可见 profile 火焰图）

**改造内容**（已实施）：
1. `utils/sentry.ts` 的 `Sentry.init` 补充配置：
   - `enableNativeFramesTracking`（默认开启，确认未被禁用）——采集慢帧/冻帧并挂到事务上。
   - `enableAppStartTracing` —— 自动采集冷/热启动 App Start span。
   - `profilesSampleRate`：按 `tracesSampleRate` 的相对比例设置（如 0.5，即整体 10% 事务带 profile），用于定位慢事务的函数级原因；额度紧张时优先保 traces。
   - `tracesSampleRate` 从 0.2 重新评估：灰度期可临时调至 0.5 积累基线，稳定后回落至 0.1~0.2。
2. release/dist 与 sourcemap 对齐：确认每次发版 `release` 字段一致（Sentry config plugin 自动上传 sourcemap 的现状已关闭，性能事务的符号化依赖 release 命名正确，需一并核对）。

**验收标准**：Sentry Performance 面板出现 App Start、导航切换事务，事务上带 frames 数据；慢事务可查看 profile 火焰图。

### P0-2 关键链路自定义埋点（Span）

**状态**：[x] 已完成（实施备注：新增 `utils/perfTracing.ts` 统一封装 `traceAsync`/`traceSync`/`startManualSpan`/`reportFirstScreen`，DSN 缺失时 SDK 自动 no-op；jest-setup.js 补齐 Sentry span API mock。实际埋点与原表略有差异：① 首屏完成用 `reportFirstScreen`（以 bundle 加载时刻为 span 起点、仅首次生效）在 fetchBills 首次完成时上报，未改 Main.tsx；② 记账落库实际在 List.tsx 乐观更新链路（非 billStore），`bill.submit` 终点为待同步落盘完成，网络同步单计 `bill.submit.network`，另增 `bill.update`/`bill.update.network` 覆盖编辑链路；③ bill.load 根 span 带 cacheHit/items/dataState 属性，子 span 为 `bill.load.cache`/`bill.load.network`；④ config.sync 根 span + `config.sync.pull`/`config.sync.push` 子 span；⑤ auth.login 手动 span，覆盖公钥获取失败/加密失败/成功/失败全部退出点；⑥ bill.parse 包裹剪贴板解析。验证：tsc 无新增错误，83 个既有测试全部通过（App.test.tsx 失败为 reanimated/worklets jest 环境的既有问题，与本改动无关）

在以下位置用 `Sentry.startSpan` / `startSpanManual` 包裹，事务命名统一 `op` 规范：

| 链路 | 位置 | span 命名 |
| --- | --- | --- |
| 首屏完成 | `pages/Main.tsx` / List 首次数据渲染完成处 | `ui.first-screen` |
| 记账提交 | `pages/List.tsx` 快速记账提交 → `stores/billStore.ts` 落库 | `bill.submit`（子 span：本地落库 / 网络同步） |
| 账单加载 | `fetchBills`（缓存读取 / 网络请求 / setData 三段） | `bill.load` |
| 配置同步 | `hooks/useConfigSync.ts` | `config.sync` |
| 登录 | `pages/Login.tsx` | `auth.login` |
| 自动记账解析 | `hooks/useAutoBookkeeping.ts` → `services/parser` | `bill.parse` |

**验收标准**：Sentry 可按事务名聚合查看各链路 P50/P75/P95 耗时与趋势。

### P0-3 性能告警规则

**状态**：[ ]

在 Sentry 后台（非代码）配置：
1. App Start P75 > 2.5s → 告警。
2. 任一关键事务 P75 周环比劣化 > 30% → 告警。
3. 崩溃率（crash-free sessions）< 99.5% → 告警。
4. 告警渠道接现有团队 IM/邮件。

### P0-4 本地性能巡检工具（开发期）

**状态**：[ ]

1. `pages/DebugTools.tsx` 增加「性能面板」入口：展示当次会话的 JS FPS 采样、内存占用（`performance.memory`/原生桥接）、AsyncStorage 总键值大小。
2. 建立手动巡检 SOP：每个 release 前用 Flashlight（或 RN 自带 Perf Monitor）跑「冷启动 → 快速记账 5 笔 → 长列表滚动」固定脚本，记录结果到发版记录。

---

## 四、性能优化计划

### P1-1 启动速度优化

**状态**：[ ]

**现状问题**：
- `App.tsx` 中所有页面（About、DebugTools、Personalization、RecurringBills 等低频页）随入口静态 import，全部进入首包。
- 启动路径上 AsyncStorage 串行读取（token、用户信息、分类配置、账单缓存等）未度量。

**改造方案**：
1. 低频页面改用 `React.lazy` + Suspense（或导航 `getComponent`）懒加载，减小首包 JS 体积与初始化耗时；Statistics 页的 gifted-charts 随页面懒加载。
2. 启动关键读取并行化：token / 用户 / 配置读取改为 `Promise.all`；将首屏必需的账单缓存预热提前，非首屏数据（模板、重复账单、提醒设置）延迟到首帧后（`requestIdleCallback` / `InteractionManager.runAfterInteractions`）。
3. 用 P0-2 的 `ui.first-screen` span 验证收益，目标冷启动 P75 < 1.5s。

**验收标准**：冷启动 span P75 较基线下降 ≥ 20%，登录态用户首屏无白屏闪烁。

### P1-2 列表与统计页渲染优化

**状态**：[ ]

**现状问题**：
- 账单量增长后 FlatList 渲染成本线性上升；`removeClippedSubviews` 因与 reanimated 动画冲突未开启（历史结论，需在新版本复验）。
- `pages/Statistics.tsx` 图表页（gifted-charts + svg）数据量大时渲染重。

**改造方案**：
1. 评估迁移 `FlashList`（Shopify，对超长列表内存与滚动性能更优）；若与现有 reanimated layout 动画冲突，则保留 FlatList 并细化 `getItemLayout` / key 策略。
2. 统计图表：月份数据先在 JS 侧聚合为纯数据再传入图表组件；图表组件 `React.memo` + 切换月份时骨架屏过渡，避免大图表阻塞交互。
3. 长列表分档验证：构造 500/2000/5000 条数据，用 P0-4 巡检脚本测滚动 FPS，目标 ≥ 55。

**验收标准**：2000 条账单滚动帧率达标；切月份统计图表不阻塞手势。

### P1-3 数据层（AsyncStorage）读写优化

**状态**：[ ]

1. 审计启动路径上的 AsyncStorage 调用次数与耗时（DebugTools 性能面板输出），合并碎读写（如多处分别读写用户配置 → 统一 key + zustand 订阅写回）。
2. 写路径防抖：configSync / 账单落盘等高频写操作合并批处理，避免主线程被序列化 JSON 挤占。
3. 大 value 治理：账单缓存 key 若单值过大，按月拆分，避免单 key 读阻塞首屏。

### P2-1 内存治理

**状态**：[ ]

1. Instruments / Android Profiler 巡检「列表 ↔ 统计 ↔ 记账弹窗」反复切换场景，排查未释放的定时器、监听器（重点：`refreshBus`、通知监听、AnimatedNumber 相关）。
2. 图表页离开时卸载图表组件（非活跃 Tab 不保留重图表实例）。
3. 图片：账单图片/头像统一走 `expo-image`（已引入）并设置缓存策略与尺寸上限，禁止直接渲染原图大图。

### P2-2 包体积治理

**状态**：[ ]

1. 依赖审计：`node-forge`（加密）、`react-native-reanimated-carousel`、`@react-native/new-app-screen` 等确认是否仍在使用，可移除项清理。
2. 产物基线：记录当前 Android release APK 与 iOS IPA 大小作为基线，后续每版本对比；Android 开启 App Bundle 分发。
3. 资源审计：`assets/` 下 png 压缩/转 webp，iconfont 保持单一来源。

### P2-3 网络与同步链路

**状态**：[ ]

1. 结合既有「同步链路无限调用」教训（导航栈堆积 + 限流缺失），为 configSync / billSync 增加统一的节流与去重，事务埋点验证线上无重复同步尖刺。
2. 请求超时与重试策略统一收敛到 `request.ts`，慢请求（> 3s）通过 span 数据暴露。

---

## 五、实施路线图

| 阶段 | 内容 | 产出 |
| --- | --- | --- |
| 第 1 阶段（P0，约 1 周） | 监控体系：P0-1 ~ P0-4 | 线上性能数据开始积累，告警生效 |
| 第 2 阶段（P1，约 2~3 周） | 启动、列表/图表、数据层优化 | 各指标达到第二节目标阈值 |
| 第 3 阶段（P2，按需排期） | 内存、包体积、网络治理 | 版本间指标不劣化 |

**每阶段收尾动作**：对比 Sentry 基线数据，更新本文档状态与实施备注（参照 `LIST_TRANSITION_OPTIMIZATION.md` 的备注格式）。

---

## 六、回归验证清单

- [ ] Sentry Performance 面板可见 App Start / 导航 / 自定义事务，帧率数据正常上报
- [ ] 告警规则触发演练通过（可人为制造慢启动验证）
- [ ] 冷启动 P75 达标；登录态/未登录态首屏均无异常
- [ ] 快速记账、滑动删除、撤销等既有动效功能不受性能改造影响（对照 LIST_TRANSITION_OPTIMIZATION.md 回归清单）
- [ ] 2000+ 条账单滚动帧率达标，无内存持续增长
- [ ] iOS + Android 双端真机验证，低端机（Android 中低端）重点抽测
- [ ] release 产物 sourcemap/dSYM 符号化正确（性能事务与崩溃堆栈均可还原）
