# 无痛记账体验优化计划

> 目标：降低手动记账的操作成本与心理负担，让用户"记一笔 < 3 秒"，并尽可能减少"手动"本身。
>
> 本文档为该优化方向的长期开发主线，按阶段推进，完成项以 `[x]` 标记。

## 现状盘点

已有的相关能力（开发时可直接复用/扩展）：

| 能力 | 位置 | 说明 |
|---|---|---|
| 剪贴板自动识别账单 | `hooks/useAutoBookkeeping.ts` | App 回到前台时读取剪贴板，解析微信/支付宝账单文本 |
| 账单文本解析器（策略模式） | `services/parser/` | `BillParser` 注册 `AlipayStrategy` / `WeChatStrategy`，天然支持扩展新策略 |
| 解析结果结构 | `services/parser/types.ts` | `ParsedBill` 已含 `category`（猜测分类）字段，目前未被充分利用 |
| 自定义数字键盘 | `components/Keypad.tsx` | 记账金额输入 |
| 记住上次记账日期 | `components/BillForm.tsx` | AsyncStorage key: `LAST_BILL_DATE` |
| 离线记账队列 | `utils/storage.ts` | `pending_bills_user:{account}` 离线缓存，登录后恢复 |
| 账单状态管理 | `stores/billStore.ts` | zustand store，含提交/编辑/刷新逻辑 |

## 优化方向与任务清单

### P0 - 阶段一：低成本高收益（纯前端改造）

#### 1. 高频分类置顶
- [x] 记录分类使用频次/最近使用时间（AsyncStorage，LRU）
- [x] `BillForm` 打开时按 LRU 排序分类，最近使用的 3~5 个排最前
- [x] 替代当前"永远从 `categories[0]` 开始"的默认行为

#### 2. 剪贴板识别增强
- [x] **去重**：对 `rawText` 做哈希，已导入过的账单不再重复弹窗
- [x] **防抖**：同一内容短时间内不重复触发（`useAutoBookkeeping.ts` 中已有 TODO 注释）
- [x] **分类预填**：实现"关键词 → 分类"映射（如 滴滴/地铁→交通，美团/饿了么→餐饮），填充 `ParsedBill.category`，导入时用户只需点"确认"
- [ ] 补充银行短信、云闪付等解析策略
- [x] 通用正则兜底策略（匹配"支付/消费 + 金额"模式），对应 `BillParser.parse` 中"此处略"的预留点

#### 3. 提交体验优化
- [x] 记账成功后用轻量 Toast + **撤销（Undo）**，代替任何二次确认
- [ ] 分类不确定时允许记入"未分类"，事后在统计页批量归类，不打断记账心流

### P1 - 阶段二：消灭重复劳动

#### 4. 周期账单
- [ ] 支持设置周期账单（房租/会员订阅/话费等）：金额、分类、周期（月/周/年）、起始日
- [ ] 到期自动生成账单（本地生成，复用现有 `addBill` 链路）
- [ ] 周期账单管理页（查看/暂停/删除）
- [ ] 生成前可选"确认模式"或"静默模式"

#### 5. 快捷模板 / 一键记账
- [ ] 用户自定义模板（如"早餐 ¥12"、"地铁 ¥4"）
- [ ] 首页提供模板快捷入口，一次点击完成记账

#### 6. 漏记提醒与补账
- [ ] 本地通知：每晚固定时间提醒（可配置开关与时点）
- [ ] 漏记检测：当天零记录且用户有记账习惯时轻提示
- [ ] 连记模式：一次连续录多笔，最后统一提交

### P2 - 阶段三：自动化入口

#### 7. 截图 / 小票 OCR
- [ ] 支付成功页截图、购物小票拍照 → OCR 提取金额与商户
- [ ] OCR 结果接入现有 parser 管道（iOS 可用端侧 Vision 框架）

#### 8. 语音记账
- [ ] 长按记账按钮语音输入（如"午饭 35 块"）
- [ ] ASR + 解析，扩展为 parser 的 `NaturalLanguageStrategy`

#### 9. 系统级入口（不打开 App 也能记）
- [ ] iOS：桌面小组件（WidgetKit）快捷记账
- [ ] iOS：App Shortcuts / Siri（"用开开记一笔午饭 30"）
- [ ] iOS：Share Extension（从账单详情页分享文本进来解析）
- [ ] Android：桌面 Widget / Share Intent
- [ ] Android：通知监听服务（读取支付通知自动记账，最接近全自动的方案）

### P3 - 阶段四：激励与心智

#### 10. 习惯激励
- [ ] 连续记账天数（streak）展示
- [ ] 记账里程碑轻反馈（非强制、不打扰）

#### 11. 预算辅助
- [ ] 月度/分类预算设置
- [ ] 超支轻提醒（记账时或统计页展示）

## 优先级总览

| 优先级 | 功能 | 理由 |
|---|---|---|
| ⭐⭐⭐ | 高频分类置顶 + 剪贴板去重/分类预填 | 纯前端改造，成本低收益立竿见影 |
| ⭐⭐⭐ | 周期账单 | 直接消灭固定支出类手动记账 |
| ⭐⭐ | 快捷模板 / Widget | 高频场景体验质变 |
| ⭐⭐ | Android 通知监听 / iOS Siri Shortcuts | 平台红利，竞品护城河 |
| ⭐ | 语音记账 / OCR | 锦上添花，技术成本稍高 |

## 开发约定

- 每完成一个任务，更新本文档对应 checkbox，并在下方"开发日志"追加一条记录。
- 涉及新依赖（如 OCR、WidgetKit）先在开发日志中记录选型结论，再动手。
- 数据持久化统一走 AsyncStorage，遵循现有用户隔离约定（key 带 account 前缀，参考 `utils/storage.ts`）。
- 新 parser 策略遵循 `services/parser/` 的策略模式，并补充对应单测（参考 `__tests__/BillParser.test.ts`）。

## 开发日志

| 日期 | 内容 |
|---|---|
| 2026-08-10 | 创建本计划文档，确定 P0~P3 四阶段路线 |
| 2026-08-10 | 完成 P0 阶段一主体：① 高频分类置顶（`utils/categoryUsage.ts` LRU，key 带 account 前缀；`BillForm` 按最近使用排序并默认选中）；② 剪贴板去重（djb2 哈希存 `clipboard_seen_hashes:{account}`）+ 60s 防抖 + 分类预填（`services/parser/categoryGuess.ts` 关键词映射）+ 通用兜底策略（`GenericStrategy`），并在 `Main.tsx` 重新启用自动记账弹窗；③ 记账成功轻量 Toast + 撤销（撤销已同步账单时调用 `deleteBill` 补偿删除；请求在途时撤销也会补偿）。新增单测 `__tests__/PainlessBookkeepingP0.test.ts`（全量 48 用例通过）。遗留：银行短信/云闪付策略、"未分类"批量归类待后续迭代。 |
